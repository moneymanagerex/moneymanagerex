wx*.blk files were copied from installation of wxWidgets.
The default location is <wxWidgets_root>\build\bakefiles\wxpresets\presets.

setbuilddir.bkl downloaded from www.bakefile.org, but it was modified and
now diverges with original version.
