# How to build (and install) .pet package
#
# To build puppy package just execute build_pet.sh from current directory
# Check version before start edit it if needed
# gtkdialog should be installed



